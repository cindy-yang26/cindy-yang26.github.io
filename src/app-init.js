initializeRandomMode();
initializeTheme();
initializeGameSwitcher();

renderInternshipCarousel(internshipIndex);

// Initialize games after all modules are loaded.
initializeGame();
initializeFlavorGame();
initializeTrainGame();
initializeSlotsGame();
